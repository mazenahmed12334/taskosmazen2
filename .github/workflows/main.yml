on:
  push:
      branches:
        - newmaster  
    





jobs:
  build:
      runs-on: windows-latest 
      
      steps:
        - name: 'Before'
          run: ls

        - uses: actions/checkout@v3

         - name: 'after'
          run: ls
        
